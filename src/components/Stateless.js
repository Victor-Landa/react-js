import React from 'react';

// Este componente solo sirve para mostrar lo que necesitamos 'visualmente'.
// Aquí no vamos a tener estado ni ciclo de vida.
const Stateless = () => {
    // Aqui sólo vamos a retornar el elemento HTML que necesitamos.
    return(
        <h1>Hola Mercurio</h1>
    );
};

export default Stateless;